# Copy this file to .env.local and put your real key there.
# .env.local is gitignored. Do not commit a real key.
# Do not rename this to VITE_TYPESAFE_API_KEY — that prefix is sent to the browser.
TYPESAFE_API_KEY=
SITES_PROJECT_ID=
